// + Operator
students = students + 1;
students += 1;
students++;

// - Operator
students = students - 1;
students -= 1;
students--;

// * Operator
students = students * 2;
students *= 2;
students**; // 이렇게 쓸 수는 없음

// / Operator
students = students / 2; -> truncated decimal places
students /= 2;

// Modular
int remainder = students % 3;
cout << remainder << endl;
